export const initialState = {
    cartItems: []
}

export const cartReducer = (state, action) => {
    switch(action) {
        case 'ADD_TO_CART': {
            const {  } = action.payload
        }
    }
};